* split projects
** semantics
** semantics-dev (can run tests) -- or put runTest/dohConfig someplace else?
** demo

* add description, contract, test for keyExtension in format / parse

* npm? cpm? keep git modules?


done
----
test on all platforms
SauceLab experiments are negative.
* Chrome tests run and finish, but there is no test reporting (0/0 tests passed).
* Chrome tests take approx. 2 times as long as local
* All other tests "do stuff", but don't "end". No idea why.
* All other tests are _very_ slow.
